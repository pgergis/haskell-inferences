(ns rules-clj.inference)

(defn pretreat
  [rules]
  (comment "COMPUTE and RETURN Pre-treated data structure"))

(defn inferoutputs
  [pretreated input-assertions]
  (comment "COMPUTE and RETURN valid inferences"))
